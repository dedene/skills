interface:
  display_name: "Ultracode"
  short_description: "Coordinate scoped workflows with resumable state"
  default_prompt: "Use $ultracode to complete the requested work within my stated scope and authority."
policy:
  allow_implicit_invocation: false
